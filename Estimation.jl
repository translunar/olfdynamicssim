using LinearAlgebra

imu_params = (arw = 0.06, #Angle random walk in deg/sqrt(hour)
              bias_instability = 0.8 #Bias instability in deg/hour
)

function imu_model(x,imu_params)

end

function state_estimator()

end
